
main.py portNum

将本地22端口远程转发到服务器的portNum端口
一个小时关闭进程之后重新运行一次,防止因为网络断开后无法重新连接
然后这个脚本文件可以直接弄成开启自动启动

如果直接运行 main.py 默认执行的话,请修改 configTunnel.py 里面的参数
需要配置用户跟端口

在启动这个端口转发程序前必须确认当前用户有到服务器上的公钥
没有的话,请自行 ssh-copy-ip 解决
